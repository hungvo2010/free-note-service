image: gradle:jdk17

stages:
  - build
  - deploy

build:
    stage: build
    script:
      - echo "Building the project..."
      - ./gradlew build
deploy:
    stage: deploy
    script:
      - echo "Deploying the project..."
    only:
        - main